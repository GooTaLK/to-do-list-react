const URL = 'http://localhost:5000';

const TASKS = URL + '/tasks';
const TASK = TASKS + '/';
const PROJECTS = URL + '/projects';
const PROJECT = PROJECTS + '/';

const data = {
	URL,
	TASKS,
	TASK,
	PROJECTS,
	PROJECT,
};

export default data;
